# AGPK-MARKET human long-form docs

Long tutorials live here (not inside install tarballs).

Layout: `docs/packages/<package_id>/USAGE.md`

Agents discover URIs via package `agpk.json` → `docs` and `list_commands.faces.docs`.
